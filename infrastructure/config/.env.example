# 环境配置示例文件
# 复制此文件为 .env 并填入实际值

# ========== 应用环境 ==========
# 可选值: test, dev, staging, prod
APP_ENV=dev

# ========== 开发环境数据库（SQLite）==========
DEV_DB_PATH=data/dev.db

# ========== Staging 环境数据库（Supabase）==========
# 格式: postgresql://user:password@host:port/database
# STAGING_DATABASE_URL=postgresql://postgres:your_password@db.xxx.supabase.co:5432/postgres
# STAGING_DB_POOL_SIZE=10
# STAGING_DB_MAX_OVERFLOW=20

# ========== 生产环境数据库（Supabase）==========
# PROD_DATABASE_URL=postgresql://postgres:your_password@db.yyy.supabase.co:5432/postgres
# PROD_DB_POOL_SIZE=20
# PROD_DB_MAX_OVERFLOW=40

# ========== 日志配置 ==========
# 日志后端会根据 APP_ENV 自动选择：
#   - test/dev → loguru（本地日志）
#   - staging/prod → logfire（云端监控）
#
# 如需手动指定，取消注释：
# LOG_BACKEND=loguru     # loguru | logfire
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# ========== 应用配置 ==========
APP_NAME=MyDDDApp
APP_VERSION=1.0.0
DEBUG=true
